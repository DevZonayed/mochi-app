# dependencies
node_modules/
.pnpm-store/

# builds / generated
dist/
dist-electron/
build/
web-build/
release/
.turbo/
*.tsbuildinfo

# …but DO keep the desktop app's committed build resources (app icon +
# generator) that electron-builder consumes when packaging.
!apps/desktop/build/
!apps/desktop/build/**

# expo / metro
.expo/
.expo-shared/

# databases (runtime)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm

# operator-local skills installed via add_skill_to_project
.claude/

# env / secrets
.env
.env.*
!.env.example

# tooling / local state
.context/
.continuum/
.playwright-cli/
.DS_Store
*.log
coverage/
